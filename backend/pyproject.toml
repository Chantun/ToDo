[project]
name = "ToDo"
version = "0.1.0"
requires-python = ">=3.14"

dependencies = [
  "fastapi>=0.139.0",
  "uvicorn[standard]>=0.51.0",
]

[dependency-groups]
dev = [
  "pytest",
  "ruff",
  "mypy",
]

[project]
name = "ToDo"
version = "0.2.0"
requires-python = ">=3.14"

dependencies = [
  "fastapi[standard]>=0.139.0",
  "uvicorn[standard]>=0.51.0",
  "pwdlib[argon2]>=0.3.0",
  "pyjwt>=2.13.0",
  "python-dotenv"
]

[dependency-groups]
dev = [
  "pytest",
  "ruff",
  "mypy",
]
